name: tests
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --upgrade pip
      - run: pip install torch --index-url https://download.pytorch.org/whl/cpu
      - run: pip install numpy Pillow opencv-python-headless scipy PyYAML tqdm huggingface_hub safetensors onnx onnxruntime pytest
      - run: python -m compileall -q .
      - run: pytest -q
      - run: python scripts/smoke_test.py
