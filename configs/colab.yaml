common:
  seed: 1021
  deterministic: true
  image_size: 352
  workers: 2
  amp: true
  grad_clip: 1.0
  use_curve_context: true
  validation_thresholds: [0.25, 0.35, 0.50, 0.65]
  cache_root: /content/.cache/lines-curves
  natural_root: /content/Lines-curves/data/natural
  curveml_root: /content/Lines-curves/data/curveml
  output_root: /content/Lines-curves/outputs
  teed_checkpoint: null

stage1:
  epochs: 2
  samples_per_epoch: 10000
  synthetic_fraction: 1.0
  batch_size: 16
  lr: 0.001
  weight_decay: 0.0002
  initial_checkpoint: null

stage2:
  epochs: 6
  samples_per_epoch: 12000
  synthetic_fraction: 0.70
  batch_size: 12
  lr: 0.00035
  weight_decay: 0.0002
  initial_checkpoint: null

stage3:
  epochs: 2
  samples_per_epoch: 3000
  synthetic_fraction: 0.0
  batch_size: 12
  lr: 0.00005
  weight_decay: 0.0001
  initial_checkpoint: null

loss:
  edge: 1.0
  curve: 1.0
  dice: 0.35
  cldice: 0.20
  consistency: 0.15
  side: 0.20
